Joins
   x Parse local column into Relation data type
   x Parse related columns into local struct
   x rename columns with join key
   x extend json parser to pull out joined columns
   x one to one
   x one to many - row number ?
   x many to many - row number ?
   x allow join types to be extended
   x join to join 
      - nest parser logic 
         - save join row name in join struct
         - use join row to look up row number i.e. list index
      - where should parser sit?
         - should join struct get nested so join.parser calls itself?

Imports x
Migrations
Updates
Imports - Joins

LazyLoading (dont parse until specific model is accessed)